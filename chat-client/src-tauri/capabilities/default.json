{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力 - 允许使用 Tauri 核心默认权限（IPC / 事件 / 窗口操作）",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
